<!-- eslint-disable -->
<template>
  <div>
    <h2>--font-ui</h2>
    <p class="font-ui" lang="zh-CN">如何用可理解并易于访问的形式在网页中展示表格化数据一向都是个不小的挑战。这个模块涵盖了基本的表格标记，以及一些更复杂的特性，比如实现标题和总结等。</p>
    <p class="font-ui" lang="en">Representing tabular data on a webpage in an understandable, accessible way can be a challenge. This module covers basic table markup, along with more complex features such as implementing captions and summaries.</p>
    <h2>--font-zh</h2>
    <p class="font-zh" lang="zh-CN">如何用可理解并易于访问的形式在网页中展示表格化数据一向都是个不小的挑战。这个模块涵盖了基本的表格标记，以及一些更复杂的特性，比如实现标题和总结等。</p>
    <p class="font-zh" lang="en">Representing tabular data on a webpage in an understandable, accessible way can be a challenge. This module covers basic table markup, along with more complex features such as implementing captions and summaries.</p>
    <h2>--font-en</h2>
    <p class="font-en" lang="zh-CN">如何用可理解并易于访问的形式在网页中展示表格化数据一向都是个不小的挑战。这个模块涵盖了基本的表格标记，以及一些更复杂的特性，比如实现标题和总结等。</p>
    <p class="font-en" lang="en">Representing tabular data on a webpage in an understandable, accessible way can be a challenge. This module covers basic table markup, along with more complex features such as implementing captions and summaries.</p>
    <h2>--font-mono</h2>
    <p class="font-mono" lang="zh-CN">如何用可理解并易于访问的形式在网页中展示表格化数据一向都是个不小的挑战。这个模块涵盖了基本的表格标记，以及一些更复杂的特性，比如实现标题和总结等。</p>
    <p class="font-mono" lang="en">Representing tabular data on a webpage in an understandable, accessible way can be a challenge. This module covers basic table markup, along with more complex features such as implementing captions and summaries.</p>
  </div>
</template>
<!-- eslint-enable -->

<script>
export default {
  setup() {
    //
  },
};
</script>

<style
    lang="scss"
    scoped>
  .font-ui {
    font-family: var(--font-ui);
  }

  .font-zh {
    font-family: var(--font-zh);
  }

  .font-en {
    font-family: var(--font-en);
  }

  .font-mono {
    font-family: var(--font-mono);
  }
</style>
